query Team {
  PersonItems(starts_with: "team/", sort_by: "position") {
    items {
      id
      position
      content {
        _uid
        firstName
        role
        image {
          filename
          alt
        }
      }
    }
    total
  }
}
