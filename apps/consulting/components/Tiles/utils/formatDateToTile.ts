import { TLibraryTile } from '@quansight/shared/types';

export const formatDateToTile = (tile: TLibraryTile): string => {
  const date = tile.content.publishedDate;
  console.log(date);

  return ``;
};
