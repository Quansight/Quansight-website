import { FC } from 'react';

import { TBlogHeaderPostDataProps } from '../types';

export const BlogHeaderPostData: FC<TBlogHeaderPostDataProps> = ({
  postTitle,
  publishedDate,
}) => {
  return <div>BlogHeaderPostData</div>;
};
