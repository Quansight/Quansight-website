import { FC } from 'react';

import { TBlogHeaderAuthorProps } from '../types';

export const BlogHeaderAuthor: FC<TBlogHeaderAuthorProps> = ({
  firstName,
  lastName,
  githubNick,
  githubLink,
  authorImage,
}) => {
  return <div>BlogHeaderAuthor</div>;
};
