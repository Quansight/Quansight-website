import { FC } from 'react';

import { TFilterTypesProps } from '../types';

export const FilterTypes: FC<TFilterTypesProps> = ({
  postType,
  postTypes,
  setPostType,
}) => {
  return <div>FilterType</div>;
};
