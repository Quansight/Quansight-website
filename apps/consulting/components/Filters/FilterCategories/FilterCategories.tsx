import { FC } from 'react';

import { TFilterCategoriesProps } from '../types';

export const FilterCategories: FC<TFilterCategoriesProps> = ({
  postCategory,
  postCategories,
  setPostCategory,
}) => {
  return <div>FilterCategories</div>;
};
